name: registro_ponto_agro_cana_forte
description: Aplicativo de registro de ponto para Agro Cana Forte

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.1.1
  sqflite: ^2.3.0
  path: ^1.8.3
  qr_code_scanner: ^1.0.1
  camera: ^0.10.5
  permission_handler: ^11.0.1
  connectivity_plus: ^5.0.1
  workmanager: ^0.5.1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true